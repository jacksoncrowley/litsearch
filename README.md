## litsearch

`litsearch` is an agent that scans research literature and returns personalised summaries based on user-defined keywords. 

## future directions
- prompt the user to fill in sentences on their research interests to auto-fill the toml file.
- calibrate weighting by passing ~10 papers to the user with scores and have them score them themselves
